You are building a Neo4j knowledge graph from a structured dataset export.

The node types have already been decided. Your job is to identify relationships between them.

NODE TYPES ALREADY DECIDED:
{node_types_json}

DATASET STRUCTURE SUMMARY:
{structure_summary}

SAMPLE RECORDS (pay attention to the "associations" and "details" fields):
{sample_records_json}

PRE-IDENTIFIED ASSOCIATION EDGE NAMES:
{pre_identified_edge_names}

These edge names were found in the sample's association array. You MUST include
every one of them in relationship_types — assign a SCREAMING_SNAKE_CASE maps_to,
and set from_type and to_type from the node types above. Do not omit any edge name
from this list, even if the same target type also appears in a path_fk_relationship.

TASK — return a JSON object with two keys:

1. "relationship_types": explicit edges from the associations array in the data.
   You MUST include every edge name listed under PRE-IDENTIFIED ASSOCIATION EDGE NAMES
   above. Map each to a SCREAMING_SNAKE_CASE relationship type with from_type and
   to_type set to canonical labels from NODE TYPES ALREADY DECIDED.
   Example: {{"name": "covers", "maps_to": "COVERS", "from": "TestCase", "to": "Requirement"}}

2. "implicit_relationships": top-level record fields that are direct foreign key
   references to another node (e.g. a field ending in "UniqueId" or "Id" at the
   root of the record). For the same type of ID reference found *inside* a nested
   array, use path_fk_relationships instead (see item 3b below).
   Example: {{"description": "test step references a module", "pattern":
   "step_uses_module", "edge_name": "moduleUniqueId", "maps_to": "USES_MODULE",
   "from_type": "TestStep", "to_type": "Module", "cross_dataset": false,
   "target_dataset_id": null}}

3. "path_fk_relationships": fields inside nested arrays that reference other
   records. Two sub-cases:

   a) PATH-BASED: the fk_field contains "/" -separated path strings matching
      another record's nodePath. Set target_field to "nodePath".
      Set container_path to the dot-path of the array, or null if the field is
      directly on the record.
      Example — entries inside an ExecutionList have "testCaseNodePath" matching
      TestCase nodePaths:
      {{"container_path": "details.entries", "fk_field": "testCaseNodePath",
      "target_field": "nodePath", "maps_to": "REFERENCES_TEST_CASE",
      "from_type": "ExecutionList", "to_type": "TestCase"}}

   b) ID-BASED: the fk_field inside a nested array contains raw uniqueId values
      referencing another node. Fields ending in "UniqueId" or "Id" inside nested
      arrays are the signal. Set target_field to "uniqueId". Set container_path to
      the dot-path of the array. Set to_type to "" if the partner could be multiple
      concrete types.
      Example — each item inside order.lineItems has "productId" pointing to a Product:
      {{"container_path": "order.lineItems", "fk_field": "productId",
      "target_field": "uniqueId", "maps_to": "CONTAINS_PRODUCT",
      "from_type": "Order", "to_type": ""}}

   For EACH field listed under "Nested array-of-object fields" in the structure
   summary, inspect the sample to see whether items contain fields ending in
   "UniqueId" or "Id". If yes, propose a path_fk_relationship with
   target_field: "uniqueId".

Return ONLY this JSON object — no explanation, no markdown, no extra text.
Always include all four keys: "relationship_types", "implicit_relationships", "association_config", and "path_fk_relationships".

{{
  "relationship_types": [
    {{"name": "<edgeName from associations>", "maps_to": "<SCREAMING_SNAKE_CASE>", "from": "<from node label>", "to": "<to node label>"}}
  ],
  "implicit_relationships": [
    {{"description": "<what this FK means>", "pattern": "<short_snake_case_name>", "edge_name": "<field name>", "maps_to": "<SCREAMING_SNAKE_CASE>", "from_type": "<canonical label of the source node>", "to_type": "<canonical label of the target node>", "cross_dataset": false, "target_dataset_id": null}}
  ],
  "association_config": {{
    "array_field": "<field name of the edge list, e.g. 'associations', 'links', 'edges', 'relationships'>",
    "edge_name_subfield": "<field within each edge object that names the relationship type, e.g. 'edgeName', 'rel', 'type'>",
    "partner_id_subfield": "<field within each edge object that holds the partner record's ID>",
    "direction_subfield": "<field encoding edge direction ('in'/'out'), or null if always outbound>",
    "direction_default": "out"
  }},
  "path_fk_relationships": [
    {{"container_path": "<dot-path to array, or null>",
      "fk_field": "<field holding the path string or ID value>",
      "target_field": "<'nodePath' for path-string match, 'uniqueId' for direct ID match>",
      "maps_to": "<SCREAMING_SNAKE_CASE>",
      "from_type": "<canonical label of source, or '' to use the record's own type>",
      "to_type": "<canonical label of target, or '' if multiple concrete types possible>"}}
  ]
}}

Set association_config to null if no explicit edge array exists in the data.

HIERARCHY PATH FIELD:
{hierarchy_field_note}

---

Rules:
- Only include edgeNames that actually appear in the sample associations arrays
- "from" and "to" must be canonical labels from NODE TYPES ALREADY DECIDED
- If you see no associations and no FK fields, return empty arrays — do not invent relationships
- Use SCREAMING_SNAKE_CASE for all maps_to values
- Do NOT propose the hierarchy path field as an implicit FK — its values are tree-location strings (e.g. "/Root/Folder/Entity"), not record IDs referencing other nodes
- implicit_relationships are for top-level record fields only; for the same
  pattern inside a nested array, use path_fk_relationships with
  target_field: "uniqueId" instead
- For each nested array listed in the structure summary, check whether items
  have fields ending in "UniqueId" or "Id" — if yes, propose a path_fk_relationship
- Only propose a path_fk_relationship when the path string values in the sample visibly match the nodePath values of typed records — do not guess
- Return path_fk_relationships: [] if no path-valued FK fields are found
